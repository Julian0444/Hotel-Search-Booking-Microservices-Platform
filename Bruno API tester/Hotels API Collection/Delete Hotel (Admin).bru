meta {
  name: Delete Hotel (Admin)
  type: http
  seq: 5
}

delete {
  url: http://localhost/admin/hotels/{{hotel_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  hotel_id: 
}

docs {
  Elimina un hotel.
  Requiere token de administrador.
}

settings {
  encodeUrl: true
}
