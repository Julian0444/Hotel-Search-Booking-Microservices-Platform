meta {
  name: Delete Reservation
  type: http
  seq: 7
}

delete {
  url: http://localhost:8081/reservations/{{reservation_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

docs {
  Cancela una reserva por su ID. Requiere autenticación JWT.
  Solo puedes cancelar tus propias reservas.
  Cambia el reservation_id en la URL según necesites.
}

params:path {
  reservation_id: res123
}

settings {
  encodeUrl: true
}
