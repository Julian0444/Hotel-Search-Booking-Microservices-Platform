meta {
  name: Delete Reservation
  type: http
  seq: 7
}

delete {
  url: http://localhost/reservations/{{reservation_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  reservation_id: 
}

docs {
  Cancela una reservación.
  Requiere autenticación.
}

settings {
  encodeUrl: true
}
