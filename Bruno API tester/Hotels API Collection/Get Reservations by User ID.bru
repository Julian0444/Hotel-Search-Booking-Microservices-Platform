meta {
  name: Get Reservations by User ID
  type: http
  seq: 9
}

get {
  url: http://localhost/users/{{user_id}}/reservations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  user_id: 4
}

docs {
  Obtiene todas las reservaciones de un usuario.
  Requiere token JWT.
  
  IMPORTANTE:
  - user_id debe coincidir con el ID del usuario del token (o ser admin)
  - Usuarios normales solo pueden ver sus propias reservaciones
}

settings {
  encodeUrl: true
}
