meta {
  name: Get User by ID
  type: http
  seq: 5
}

get {
  url: http://localhost/users/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  user_id: 1
}

docs {
  Obtiene un usuario por su ID.
  Requiere autenticación.
}

settings {
  encodeUrl: true
}
