meta {
  name: Delete User
  type: http
  seq: 6
}

delete {
  url: http://localhost/users/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  user_id: 1
}

docs {
  Elimina un usuario por su ID.
  Requiere autenticación de administrador.
}

settings {
  encodeUrl: true
}
